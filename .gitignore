
# Diretórios e Arquivos de Build/Cache
/build
/dist
/tmp
*.log
npm-debug.log*

# Banco de dados SQLite — reconstruível via `npm run migrate` + `npm run seed`
server/*.db

# Node.js
/node_modules
pnpm-lock.yaml
yarn.lock